{
  "name": "motionshot-studio",
  "version": "0.1.0",
  "private": true,
  "description": "Motionshot Studio — das KI-Videostudio: textbasiert schneiden, mit KI regie führen, mit Motion Graphics veredeln.",
  "scripts": {
    "dev": "concurrently -n api,web -c blue,magenta \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && uvicorn app.main:app --reload --port 8787",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "typecheck": "cd frontend && npm run typecheck && cd ../remotion && npm run typecheck",
    "test": "cd backend && python -m pytest tests/ -q",
    "studio": "cd remotion && npm run studio"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
